{
  "frontend/src/App.tsx": "Root app component with role-based routing, lazy-loaded pages, ErrorBoundary, and Suspense; fixes the type mismatch by making `navigate` accept `string` (with an `isPage` guard before calling `setCurrentPage`) so it is compatible with `Dashboard`'s `onNavigate?: (page: string) => void` prop.",
  "frontend/src/components/qr/QRScanModal.tsx": "Camera-based QR scanning modal using `useQRScanner`; removes unused eslint-disable comments by providing full dependency arrays to both `useEffect` hooks.",
  "frontend/src/pages/ScanPage.tsx": "QR scan page for admin/staff/delivery roles; fixes `useAllOrders` to pass `sessionEmail`, and fixes `updateOrder.mutateAsync` calls to use `updatedOrder` and `sessionEmail` keys matching the hook signature.",
  "frontend/src/pages/delivery/DeliveryDashboard.tsx": "Delivery dashboard with two-stage QR scanning (Dispatched→Out for Delivery with GPS, Out for Delivery→Delivered with empty truck image upload); uses the existing `QRScanModal` `onScanned` interface (no `orderId` prop) by storing the target order in state and passing the scanned value to the mutation.",
  "frontend/src/pages/admin/QRManagementPage.tsx": "Admin QR management page; fixes the `QRCodeDisplay` usage by removing the non-existent `qrValue` prop and instead passing `order.qrCode?.value ?? order.orderId` as the `orderId` prop, which the component uses to generate the QR image URL.",
  "frontend/src/pages/admin/UserManagementPage.tsx": "Admin user management page with full CRUD; imports `AddUserInput`, `mapAppRoleToBackendRole`, and `mapBackendRoleToAppRole` from `useQueries`, and fixes the `updateUserMutation.mutateAsync` call to use the correct `updatedUser` key instead of `user`.",
  "frontend/src/pages/admin/ReportsPage.tsx": "Admin reports page with order analytics; fixes `useAllOrders` and `useAllStores` calls to pass `sessionEmail` from `useAuth`.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page showing account info and sign-out; fixes `user?.name` (which doesn't exist on `AuthUser`) by deriving a display name from `user?.email`.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; restored exported helpers `mapBackendRoleToAppRole`, `mapAppRoleToBackendRole`, `AddUserInput` type, and fixed `useUpdateUser` mutation to use `updatedUser` key matching the backend interface; also includes all new hooks for dashboard stats, delivery verification, and distributor confirmation.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light/dark modes, status badge classes, card shadow utilities, and sidebar nav item styles — now with an explicit default `color` on `.sidebar-nav-item` so all nav items are always visible without requiring hover.",
  "frontend/src/components/layout/AppSidebar.tsx": "Sidebar navigation component with role-based nav items; fixes the type mismatch by changing `AppSidebarProps.currentPage` and `onNavigate` to use `string` instead of the local `Page` union, making it compatible with callers that pass plain strings.",
  "frontend/src/contexts/AuthContext.tsx": "Authentication context that now calls `actor.login(email, password)` from the backend instead of a hardcoded local user list, enabling newly created users to log in immediately; session restoration, sessionStorage sync, and backward-compat aliases are preserved.",
  "frontend/src/pages/LoginPage.tsx": "Login page with email/password form, show/hide password toggle, and inline error display; removes the unused eslint-disable comment by including `error` in the `useEffect` dependency array.",
  "frontend/src/pages/admin/StoreManagementPage.tsx": "Admin store management page with full CRUD for stores, featuring an inline satellite Leaflet map picker (Esri World Imagery tiles) in the Add/Edit dialogs for selecting store location by clicking on the map, plus a \"Use Current Location\" geolocation button.",
  "frontend/src/pages/admin/OrderManagementPage.tsx": "Admin order management page; fixes `QRPrintView` prop from `qrValue` to `qrCodeValue` to match the component's interface.",
  "frontend/src/pages/admin/DistributorDeliveryManagementPage.tsx": "Admin distributor delivery management page; fixes all hook calls to pass `sessionEmail`, fixes `updateMutation.mutateAsync` to use `updatedDelivery` key, `createMutation.mutateAsync` to use `{ delivery, sessionEmail }`, and `deleteMutation.mutateAsync` to use `{ deliveryId, sessionEmail }`.",
  "frontend/src/pages/distributor/DistributorDashboard.tsx": "Distributor dashboard; fixes `useDistributorDeliveriesByUser`, `useAllOrders`, and `useAllStores` calls to pass `sessionEmail` as required argument.",
  "frontend/src/pages/staff/OrderCreationPage.tsx": "Staff order creation page with a new order form and an Approved Orders section; uses the existing `QRScanModal` `onScanned` interface (no `orderId` prop) by storing the target order in state and passing the scanned QR value to `updateOrderStatusUsingQR`.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates the backend actor and always attempts to call `_initializeAccessControlWithSecret` with the admin token from sessionStorage, logging console.error if the token is missing or initialization fails.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page showing order stats and recent orders; fixes the `onNavigate` prop type to `(page: string) => void` (compatible with App's `Page` union type) and prefixes the unused param with `_`.",
  "frontend/src/components/qr/QRCodeDisplay.tsx": "QR code display component; refactored to use React state for error handling (replacing DOM manipulation), added a guard for empty/undefined `orderId`, and shows a clean fallback div when the QR image fails to load.",
  "frontend/src/utils/orderUtils.ts": "Complete order utility module restoring all previously-used exports including `getStatusColor`, `getStatusIcon`, `parseOrderMeta`, `buildNotesWithMeta`, `decodeQRData`, `encodeQRData`, plus new workflow helpers like `WORKFLOW_STAGES`, `isOrderLocked`, `getStatusBadgeClass`, and `validateStatusTransition`.",
  "frontend/src/components/distributor/BarcodeScanStep.tsx": "Camera-based barcode scanning component using `useQRScanner`; removes unused eslint-disable comment by providing full dependency arrays to the `useEffect` hook.",
  "frontend/src/components/orders/QRPrintView.tsx": "A print-optimized component that displays a QR code image (via qrserver.com API) and Order ID in a clean layout, with a print button that triggers window.print() and @media print CSS to hide navigation chrome.",
  "frontend/src/components/delivery/EmptyTruckImageUpload.tsx": "A dialog component that prompts delivery agents to upload an empty truck photo after marking an order as delivered, using ExternalBlob.fromBytes for upload with progress tracking via withUploadProgress.",
  "frontend/src/components/map/SatelliteMapView.tsx": "Reusable Leaflet satellite map component with loading/error states and dynamic marker updates; retains the intentional eslint-disable on the mount-only effect with an explanatory comment.",
  "frontend/src/pages/admin/LiveTrackingPage.tsx": "Admin live tracking page showing a full-width satellite Leaflet map with truck markers for all active delivery agents (orders in Out for Delivery/Dispatched status), auto-refreshing every 30 seconds with a list of active deliveries below the map.",
  "frontend/src/pages/admin/DeliveryVerificationPage.tsx": "Admin delivery verification page with extended table columns (Order Contents, Delivery Destination, Empty Truck image); fixes the `Uint8Array` to `Blob` conversion by casting `data.buffer` to `ArrayBuffer` via `as ArrayBuffer` to satisfy strict TypeScript typing.",
  "frontend/src/components/layout/Layout.tsx": "App shell layout with responsive sidebar and top bar; now passes `currentPage` as `string` directly to `AppSidebar` (which now accepts `string`), resolving the previous type error.",
  "frontend/src/components/distributor/DistributorWorkflowDialog.tsx": "Multi-step distributor delivery confirmation dialog; fixes `submitMutation.mutateAsync` to include `sessionEmail`, and fixes `BarcodeScanStep` prop from `onScanSuccess` to `onBarcodeScanned`.",
  "frontend/src/components/orders/InvoiceModal.tsx": "Invoice display modal; fixes `useAllOrders` and `useAllStores` calls to pass `sessionEmail` obtained from `useAuth`."
}