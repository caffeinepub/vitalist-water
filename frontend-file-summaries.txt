{
  "frontend/src/App.tsx": "Root app component that inlines all page routing logic (removing the missing AppLayout import), wraps everything in ThemeProvider/QueryClientProvider/AuthProvider, and renders the correct page based on user role and currentPage state.",
  "frontend/src/components/qr/QRScanModal.tsx": "Camera-based QR scanning modal using the useQRScanner hook (imported via relative path to fix the module resolution error), with live camera preview, scan overlay, success/error feedback, and automatic order ID extraction.",
  "frontend/src/pages/ScanPage.tsx": "QR scan page for admin/staff/delivery roles; updated import from `useGetAllOrders` to `useAllOrders` to match the renamed hook.",
  "frontend/src/pages/delivery/DeliveryDashboard.tsx": "Delivery dashboard; updated imports from `useGetAllOrders`/`useGetAllStores` to `useAllOrders`/`useAllStores`.",
  "frontend/src/pages/admin/QRManagementPage.tsx": "Admin QR management page; updated imports from `useGetAllOrders`/`useGetAllStores` to `useAllOrders`/`useAllStores`.",
  "frontend/src/pages/admin/UserManagementPage.tsx": "Admin user management page with full CRUD; added a \"User ID\" column to the table displaying each user's backend-assigned ID in a monospace badge, updated `handleSubmit` to use `AddUserInput` for new users (no `id` field) and preserve the existing `id` when editing, and shows the user ID inside the Edit dialog.",
  "frontend/src/pages/admin/ReportsPage.tsx": "Admin reports page with order analytics; updated imports from `useGetAllOrders`/`useGetAllStores` to `useAllOrders`/`useAllStores`.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page showing user profile info and sign-out; switched from `currentUser` to `user` from useAuth to match the updated context shape.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; fixed `useAllUsers` to pass `sessionEmail` to `actor.getAllUsers()`, fixed `useAddUser` to accept `AddUserInput` type (without `id`) and call `actor.addUser` with the correct `{ email, hashedPassword, role }` input shape so the backend generates the user ID, and exported the `AddUserInput` type.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light/dark modes, status badge classes, card shadow utilities, and sidebar nav item styles — now with an explicit default `color` on `.sidebar-nav-item` so all nav items are always visible without requiring hover.",
  "frontend/src/components/layout/AppSidebar.tsx": "Sidebar navigation; switched from `currentUser` to `user` from useAuth and updated `flex-shrink-0` to `shrink-0`.",
  "frontend/src/contexts/AuthContext.tsx": "Authentication context that now calls `actor.login(email, password)` from the backend instead of a hardcoded local user list, enabling newly created users to log in immediately; session restoration, sessionStorage sync, and backward-compat aliases are preserved.",
  "frontend/src/pages/LoginPage.tsx": "Login page with email/password form and loading state — demo credentials hint section has been removed, leaving only the clean sign-in form with email input, password input, and submit button.",
  "frontend/src/pages/admin/StoreManagementPage.tsx": "Admin store management page with full CRUD; added a \"Use Current Location\" button in the Add/Edit Store dialog that invokes the browser Geolocation API with a loading spinner, populates latitude/longitude fields on success, and shows a clear inline error message on permission denial or unavailability.",
  "frontend/src/pages/admin/OrderManagementPage.tsx": "Admin order management page with approve/cancel/dispatch/deliver actions; removed the \"Admin token is missing\" warning banner and replaced it with an actorReady guard that shows an inline spinner and disables buttons while the actor initializes.",
  "frontend/src/pages/admin/DistributorDeliveryManagementPage.tsx": "Admin distributor delivery management page; updated imports from old hook names to `useAllDistributorDeliveries`/`useAllOrders`, removed token-missing warning banner, and uses `isAdmin` from the restored auth context alias.",
  "frontend/src/pages/distributor/DistributorDashboard.tsx": "Distributor dashboard; updated imports from old hook names to `useDistributorDeliveriesByUser`/`useAllOrders`/`useAllStores`, removed Internet Identity dependency, and fixed `flex-shrink-0` to `shrink-0`.",
  "frontend/src/pages/staff/OrderCreationPage.tsx": "Staff order creation page; updated import from `useGetAllStores` to `useAllStores`.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates the backend actor and always attempts to call `_initializeAccessControlWithSecret` with the admin token from sessionStorage, logging console.error if the token is missing or initialization fails.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page showing order stats and recent orders; updated import from `useGetAllOrders` to `useAllOrders` to match the renamed hook."
}