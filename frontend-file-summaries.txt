{
  "frontend/src/App.tsx": "Root app component with role-based page routing and lazy-loaded pages; removed the `role` prop from the `Layout` usage since `Layout` derives the role internally from `useAuth()`.",
  "frontend/src/components/qr/QRScanModal.tsx": "Camera-based QR scanning modal using `useQRScanner` imported via relative path with `@ts-ignore`; auto-starts camera on open, processes first valid scan, and provides camera controls with scanning overlay.",
  "frontend/src/pages/ScanPage.tsx": "QR scan page for all roles; no `role` prop (removed), uses `useAuth()` for `sessionEmail` and `user`, and `QRScanModal` without a `description` prop.",
  "frontend/src/pages/delivery/DeliveryDashboard.tsx": "Delivery dashboard fetching assigned orders by Internet Identity principal; `EmptyTruckImageUpload` is used without the `onUpload` prop (now optional), and all `sessionEmail` references come from `useAuth()`.",
  "frontend/src/pages/admin/QRManagementPage.tsx": "Admin QR management page; fixes the `QRCodeDisplay` usage by removing the non-existent `qrValue` prop and instead passing `order.qrCode?.value ?? order.orderId` as the `orderId` prop, which the component uses to generate the QR image URL.",
  "frontend/src/pages/admin/UserManagementPage.tsx": "Admin user management page with full CRUD; imports `AddUserInput`, `mapAppRoleToBackendRole`, and `mapBackendRoleToAppRole` from `useQueries`, and fixes the `updateUserMutation.mutateAsync` call to use the correct `updatedUser` key instead of `user`.",
  "frontend/src/pages/admin/ReportsPage.tsx": "Admin reports page with order analytics; fixes `useAllOrders` and `useAllStores` calls to pass `sessionEmail` from `useAuth`.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page showing account info and sign-out; fixes `user?.name` (which doesn't exist on `AuthUser`) by deriving a display name from `user?.email`.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; `useCreateOrder` now accepts `{ input, sessionEmail }`, `useAdminDashboardStats` accepts only `sessionEmail` (single arg), and all hooks are self-contained without importing from AuthContext to avoid circular dependencies.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light/dark modes, status badge classes, card shadow utilities, and sidebar nav item styles — now with an explicit default `color` on `.sidebar-nav-item` so all nav items are always visible without requiring hover.",
  "frontend/src/components/layout/AppSidebar.tsx": "Sidebar navigation component with role-based nav items; fixes the type mismatch by changing `AppSidebarProps.currentPage` and `onNavigate` to use `string` instead of the local `Page` union, making it compatible with callers that pass plain strings.",
  "frontend/src/contexts/AuthContext.tsx": "Authentication context providing `user`, `sessionEmail`, `login`, `logout`, `isLoading`, and `error`; `sessionEmail` is derived from `user?.email ?? ''` and exposed on the context type so all consumers can destructure it directly.",
  "frontend/src/pages/LoginPage.tsx": "Login page with email/password form, show/hide password toggle, and inline error display; removes the unused eslint-disable comment by including `error` in the `useEffect` dependency array.",
  "frontend/src/pages/admin/StoreManagementPage.tsx": "Admin store management page with full CRUD for stores, featuring an inline satellite Leaflet map picker (Esri World Imagery tiles) in the Add/Edit dialogs for selecting store location by clicking on the map, plus a \"Use Current Location\" geolocation button.",
  "frontend/src/pages/admin/OrderManagementPage.tsx": "Admin order management page with full CRUD, status filtering, invoice modal, QR print, and an assign-delivery dialog that uses Principal.fromText to convert the selected user ID to a Principal for the assignDelivery mutation with proper cache invalidation.",
  "frontend/src/pages/admin/DistributorDeliveryManagementPage.tsx": "Admin distributor delivery management page with full CRUD; replaced `isAdmin` destructured from `useAuth()` with a locally derived `const isAdmin = user?.role === 'admin'` to match the updated `AuthContextType`.",
  "frontend/src/pages/distributor/DistributorDashboard.tsx": "Distributor dashboard passing `order` (not `orderId`) to `DistributorWorkflowDialog`, using `sessionEmail` from `useAuth()`, and showing pending confirmation orders with a workflow dialog trigger.",
  "frontend/src/pages/staff/OrderCreationPage.tsx": "Staff order creation page; `createOrderMutation.mutateAsync` now uses `{ input, sessionEmail }` matching the updated hook signature, and `QRScanModal` is used without the removed `description` prop.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates the backend actor and always attempts to call `_initializeAccessControlWithSecret` with the admin token from sessionStorage, logging console.error if the token is missing or initialization fails.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page showing order stats and recent orders; calls `useAdminDashboardStats(email)` with a single argument and uses `sessionEmail` from `useAuth()`.",
  "frontend/src/components/qr/QRCodeDisplay.tsx": "QR code display component that generates the image URL via generateQRCodeURL (VITALIST prefix + base64 encoding) and shows a fallback div on image load error.",
  "frontend/src/utils/orderUtils.ts": "Order utility module with `WorkflowStage` interface (key, label, step fields), `WORKFLOW_STAGES` as an array of objects, `getStageIndex` function, status color/icon helpers, QR encode/decode functions, and workflow validation helpers.",
  "frontend/src/components/distributor/BarcodeScanStep.tsx": "Camera-based barcode/QR scanning component using `useQRScanner` via relative path with `@ts-ignore`; auto-starts on mount, decodes scanned values via `decodeQRData`, and calls `onBarcodeScanned` with the decoded order ID.",
  "frontend/src/components/orders/QRPrintView.tsx": "Print-optimized QR code view using generateQRCodeURL; accepts optional qrCodeValue prop (falls back to orderId) and includes a print button with @media print styles.",
  "frontend/src/components/delivery/EmptyTruckImageUpload.tsx": "Dialog for uploading empty truck photos after delivery; `onUpload` prop is now optional (with `?`) so it can be omitted when the component handles upload internally via `useAddEmptyTruckImage`.",
  "frontend/src/components/map/SatelliteMapView.tsx": "Reusable Leaflet satellite map component with loading/error states and dynamic marker updates; retains the intentional eslint-disable on the mount-only effect with an explanatory comment.",
  "frontend/src/pages/admin/LiveTrackingPage.tsx": "Admin live tracking page showing a full-width satellite Leaflet map with truck markers for all active delivery agents (orders in Out for Delivery/Dispatched status), auto-refreshing every 30 seconds with a list of active deliveries below the map.",
  "frontend/src/pages/admin/DeliveryVerificationPage.tsx": "Admin delivery verification page with extended table columns (Order Contents, Delivery Destination, Empty Truck image); fixes the `Uint8Array` to `Blob` conversion by casting `data.buffer` to `ArrayBuffer` via `as ArrayBuffer` to satisfy strict TypeScript typing.",
  "frontend/src/components/layout/Layout.tsx": "App shell layout with responsive sidebar and top bar; replaced `currentUser` with `user` from `useAuth()` to match the updated `AuthContextType`, and removed the unused `role` prop from `LayoutProps`.",
  "frontend/src/components/distributor/DistributorWorkflowDialog.tsx": "Multi-step distributor delivery confirmation dialog with scan→upload→done flow; validates scanned barcode against expected order ID and submits truck images via submitDistributorConfirmation mutation.",
  "frontend/src/components/orders/InvoiceModal.tsx": "Invoice display modal that fetches orders and stores via React Query, finds the matching order and store by ID, shows loading skeletons and error alerts, and renders InvoiceView when data is ready.",
  "frontend/src/utils/qrGenerator.ts": "QR code generation utilities using the goqr.me API; `generateQRCodeURL` encodes the order ID with the VITALIST prefix + base64 format matching the decoder in orderUtils.ts.",
  "frontend/src/components/orders/InvoiceView.tsx": "Print-optimized invoice component rendering order details, store info, line items with GST calculation, and a QR code image; uses generateQRCodeURL from qrGenerator.ts with error fallback.",
  "frontend/src/components/orders/WorkflowStageIndicator.tsx": "Visual workflow progress indicator using `WORKFLOW_STAGES` object array with `key`, `label`, and `step` fields; supports compact and full display modes with a progress line and step circles."
}